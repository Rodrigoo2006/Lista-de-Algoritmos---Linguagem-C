#include <stdio.h>
int main() {

float c,f;
printf ("Digite a temperatura em graus fahrenheit:");
scanf ("%f", &f);

c = (f - 32) * 5 / 9;

printf("Temperatura convertida em Celsius: %.2f", c);
return 0;
}
